'use strict';

var mConstants = require('./_mConstants'),
    loader = require('../../utilities/loader');

mConstants.constant('Constants', {
    ProjectType: {
        project: 1,
        library: 2
    }
});
