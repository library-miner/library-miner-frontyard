/* eslint-env protractor */

'use strict';

describe('index', function () {
  beforeEach(function () {
    browser.get('');
  });

  it('トップページにアクセスしタイトルを取得できること', function () {
    // いつか書く
  });
});
